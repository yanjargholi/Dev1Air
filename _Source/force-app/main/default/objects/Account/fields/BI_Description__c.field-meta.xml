<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>BI_Description__c</fullName>
    <description>We are using this field to populate the business description from Hoovers, in the same way as the out of box description field. We added this custom field so that we can map the description from the lead custom description when the lead is converted and an existing account is selected, since we only have the option to map custom fields.</description>
    <externalId>false</externalId>
    <label>Description</label>
    <length>32768</length>
    <trackFeedHistory>false</trackFeedHistory>
    <trackHistory>false</trackHistory>
    <type>LongTextArea</type>
    <visibleLines>4</visibleLines>
</CustomField>
